name: refbox
on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:
  pull_request:
    paths:
      - 'deploy/refbox/**'
      - 'scripts/e2e-refbox.sh'
      - '.github/workflows/refbox.yml'
      - 'examples/echo-agent/**'
      - 'internal/**'
      - 'cmd/agenthof/**'

permissions:
  contents: read

jobs:
  refbox:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: podman
        run: |
          if ! command -v podman >/dev/null; then
            sudo apt-get update
            sudo apt-get install -y podman
          fi
          podman --version
      - run: ./scripts/e2e-refbox.sh
