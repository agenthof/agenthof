name: refbox-echo
description: Reference agent running inside a refbox compartment (UDS, no network)
model: fast
instruction: Echo the input back.
output: echo
execution: fronted
endpoint: unix:///run/agenthof/refbox-echo.sock
exec:
  mode: attested
  allow:
    - exe: "true"
